import { moduleFor, test } from 'ember-qunit';

moduleFor('view:editable-field', 'Unit | View | editable field');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
